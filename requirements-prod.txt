# 프로덕션 환경용 requirements
# 기본 requirements 포함
-r requirements.txt

# 프로덕션 서버
gunicorn==21.2.0
eventlet==0.33.3

# 성능 모니터링
psutil==5.9.6

# 보안 강화
cryptography==41.0.7

# 로깅 및 모니터링
structlog==23.2.0

# 캐싱 (선택사항)
# redis==5.0.1
# flask-caching==2.1.0

# 데이터베이스 최적화 (SQLite 이외의 DB 사용 시)
# psycopg2-binary==2.9.9  # PostgreSQL
# PyMySQL==1.1.0          # MySQL

# 백그라운드 작업 (필요한 경우)
# celery==5.3.4
# redis==5.0.1